---
description: Show memory system status and tasks
---

Check the current state of the memory system by calling memory_status() and task_list().
Show a summary of:
1. Current session count and status
2. Active and pending tasks
3. Recent achievements
4. Quality score trend

Then suggest what to work on next based on task priorities.
